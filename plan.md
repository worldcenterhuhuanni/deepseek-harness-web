# 这次要做什么（一句话主题）

## 子目标

为什么要做，一两句。

- [ ] 要做的第一件事

<!--
锚点（这次要改哪些符号）**可以不写**。
不写时 ITP 走 L0 自举：第一个功能点不判定，做完后从实际改动反向生成锚点
并回写，第二个功能点起就正常判定了（SPEC §3.1）。

只有想让「第一个功能点就参与判定」时，才需要预先写：
  - [ ] 改 `src/a.py::some_function`     符号级，判定最准
  - [ ] 改 `src/a.py`                    文件级，粒度粗

做完一项就把 [ ] 改成 [x]，判定自动触发。
-->
